apiVersion: v1
kind: Pod
metadata:
  name: rbac-test
  namespace: roboshop
spec:
  containers:
    - name: nginx
      image: nginx
---
apiVersion: v1
kind: Service
metadata:
  name: rbac-svc
  namespace: roboshop
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80